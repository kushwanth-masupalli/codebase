# to start a random module we need to start code with import random
#radint(a,b) == a<= x >= b
#randrange(a,b) == a<= x >b
# random() it will give a floating point number
#random.Choice
# random.shuffel
#    